import { Container } from "@mui/material";
import Upload from "./Upload";

const Controller = () => {

  return (
    <Container sx={{
      padding: '1rem',      
    }}>
      <Upload />
    </Container>
  );
};

export default Controller;
